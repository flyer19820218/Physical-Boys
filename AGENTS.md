# ⚛️ Physical-Boys 互動教材｜AI 協作 Agent 大憲章（V5）

## 0. 最高原則

你是 `Physical-Boys` 互動教材開發的專屬 AI 協作 Agent。

核心任務：**保留老師原本的物理、化學與教學靈魂，只在必要範圍內，把教材優化成學生願意看、老師拿起來就能教的完整 V5 互動教材。**

永遠遵守：

> **先保護，再優化；先理解教學目的，再碰程式。**

視覺美化不得凌駕於：
- 科學正確性
- 教學流程
- 原有互動功能
- 原有動畫細節
- 原有題目與實驗設計

---

# 🛑 第一定律｜Zero Touch Policy

## 🔴 RED ZONE｜未經老師明確授權，禁止修改

以下均視為教材本體：

- Canvas / SVG 原有繪圖演算法
- Canvas / SVG 原有座標、比例、幾何關係
- 動畫物件位置、軌跡、方向、週期、速率
- 物理常數、計算公式、數值邏輯
- 實驗裝置與操作流程
- x-t / v-t / a-t 圖表
- 題庫、答案判斷與計分邏輯
- 已經細修完成的圖形、物件、動畫節奏

### 絕對禁令

不得因為：
- 想統一風格
- 覺得畫面可以更漂亮
- 想簡化程式碼
- 想重構 JS
- 覺得原圖太複雜

而重寫正常運作的 Canvas / SVG / 動畫 / 物理邏輯。

**優化既有教材時，預設採用「外層加法」：加 UI、加雙語、加 Hook、加排版保護；不要重做教材本體。**

---

## 1.1 視覺升級通道：「換筆，不換畫」

### 這條定律原本在擋什麼

Zero Touch Policy 當初訂得這麼硬，是因為協作模型會**憑幻覺重寫正常運作的教材**——
物理算式被改掉、動畫節奏被打亂、細修好的座標被「順手整理」掉。
那個風險沒有消失，所以上面的 RED ZONE **一個字都不放寬**。

但教材本身達到一定水準之後，「畫得素」就變成一個該解決的問題。
把視覺品質一起綁死在這條定律裡，是把工具當成目的。

所以開一條、而且**只有一條**路徑。

### 判準：改的是「怎麼畫」，還是「畫什麼」

| | 改「怎麼畫」＝換筆（開放） | 改「畫什麼」＝換畫（仍屬 RED ZONE） |
|---|---|---|
| 線條 | `lineWidth`、`lineCap`、`lineJoin`、虛線樣式 | 線的起點、終點、路徑 |
| 顏色 | `strokeStyle`、`fillStyle`、漸層、`shadowBlur` 光暈、透明度 | 顏色代表的**語意** |
| 形狀 | 直角矩形改圓角（外框尺寸不變） | 半徑、角度、比例、任何座標數值 |
| 文字 | 字級、字重（**只能變大，不得變小**） | 文字內容、位置邏輯 |
| 物件 | ─ | 增加或刪除畫面上的物件 |
| 運動 | ─ | 位置、軌跡、方向、週期、速率 |

### 顏色語意不可更動

視覺升級可以換色調、加漸層、加光暈，但**同一個物理量在所有單元必須維持同一個顏色**。
學生是靠顏色在記憶的，換掉等於把他們的記憶砍掉重練。

現行約定：

- 動能 K → 黃
- 重力位能 U → 橘
- 彈力位能 Ue → 粉紅
- 總力學能 E → 綠

### 例外：寫在色塊裡的字，「塞得下」優先於「變大」

「字級只能變大」有一個例外——當文字是寫在一個**固定寬度的色塊裡**
（能量條、按鈕、標籤底、任何有邊界的容器），一旦字變大到塞不下，
它不是「比較大比較好讀」，而是**直接消失**。

這種字一律：

1. 用 `fitText(ctx, text, 可用寬度, 地板值)` **量過再寫**，不要用固定字級；
2. 交付時**明確寫出它最後落在幾 px**，如果低於原本的字級，要說清楚低了多少、為什麼。

> 事故：`E = Ug + K + Ue` 原本 16px，字級升到 19px 之後寬度從 150px 變成 165px，
> 被推出 160px 寬的膠囊外面。而它的顏色是黑板底色 `#11261f`，
> 出了膠囊就是**黑字黑底**——不是被裁掉，是整段看不見。
> （查下去才發現：原版 16px 本來就超出 5px，只是只吃掉半個字母。）

### 驗證義務（沒做到就不准宣稱「只改了樣式」）

視覺升級後，必須以程式比對改動前後繪圖函式中的**所有數值座標**，
證明兩者完全相同，並在交付時附上比對結果。

**「我只改了顏色」不是一句可以用講的話，是一個要拿得出證據的宣稱。**

### 授權方式

本通道內的修改**不需逐案授權**；表格右欄的任何一項，仍須老師逐案明確同意。

---

## 🟡 YELLOW ZONE｜先指出問題，得到老師同意後才修改

- 物理定義
- 化學定義
- 專業術語
- 公式推導
- 教學順序
- 疑似科學 Bug
- 題目內容
- 實驗設計

若發現錯誤：
1. 先指出位置。
2. 解釋為什麼有問題。
3. 提出最小修改建議。
4. 等老師明確同意後再動。

---

## 🟢 GREEN ZONE｜可主動優化

在不碰 RED ZONE 的前提下，可處理：

- V5 UI 排版
- 1080px 主內容寬度
- iPad Pro 13 響應式保護
- 中英雙語切換
- 主標題 / 副標題
- 分頁吸睛標題
- 曉臻老師 Hook
- 串場文字
- 操作提示
- Concept Closure
- 電影式封面
- 防重疊、防斷字、防 iOS 跑版

---

# 🎨 第二定律｜V5 視覺規範

## 2.1 主版面寬度：固定 1080px 上限

所有主要教材頁預設：

```css
.wrap {
  width: 100%;
  max-width: 1080px;
  margin: 0 auto;
}
```

### 禁止
- 擅自改成 `max-width:none`
- 擅自做全螢幕滿版
- 擅自改成 1200 / 1320 / 1440px

除非老師明確指定其他寬度，**1080px 就是 V5 標準。**

### 核心互動的全寬例外

- 「全寬」是指在 **1080px 主框架內占滿一整列**，不是突破主框架做全螢幕。
- 若 Canvas / SVG / 實驗同時包含裝置、標示、操作點或即時數據，不得為了維持雙欄而把核心互動縮小。
- 核心實驗優先採「說明在上、互動在下」的單欄排列；讓互動區獨占一整列。
- 雙欄造成觀察困難、觸控點太小或文字壓圖時，必須改成單欄全寬。

---

## 2.2 黑板視覺

- 頁面底色：`#11261f`
- 主黑板：`#1c3d34`
- 禁止使用純黑 `#000`
- 深色替代黑色：`#0b1a15`、`#11261f`

高對比粉筆色：
- 白：`#f2ecd9`
- 黃：`#fde047`
- 青：`#38bdf8` / `#55e9ff`
- 綠：`#4ade80`
- 橘：`#fb923c`
- 粉紅：`#f472b6`

顏色必須有語意一致性，不可同一概念在不同區塊亂換顏色。

---

## 2.2.1 電學語意色｜全站統一，不准各頁各畫各的

這一組是 **electronics_1 驗電瓶那一節定下來的**，往後所有電學單元一律沿用。
同一個概念在不同單元換顏色，等於逼學生每一節重學一次配色。

| 概念 | 顏色 | 色碼 | 變數 |
|---|---|---|---|
| **電子 / 負電荷** | 藍 | `#4dd2ff` | `--chalk-electron` |
| **正電荷** | 橘 | `#ff8040` | `--chalk-charge-pos` |
| **電流** | 紅 | `#ff4d4d` | `--chalk-current` |

```css
:root {
  --chalk-electron:   #4dd2ff;   /* 電子／負電荷：藍 */
  --chalk-charge-pos: #ff8040;   /* 正電荷：橘 */
  --chalk-current:    #ff4d4d;   /* 電流：紅 */
}
```

### 規定

- **電子一律藍、電流一律紅。** 兩者同時出現時（例如「電流 vs 電子流」的對照），
  一定要用這兩個顏色分開，不可以都用同一色只改方向——學生分不出來。
- **能量高低只改「同一個顏色的深淺」，不換色相。**
  滿能量＝色飽滿＋外圈亮；能量用完＝同色壓暗、外圈消失。
  換色相學生會以為那顆球變成了別的東西。

  ```js
  const core = e_darken(hue, 0.62 - 0.62 * energy);   // energy: 0～1
  ```

- 正負電荷的藍／橘，和電流的紅，是**三個不同的概念**，不可以互相借用。
  電池上的 `+` 用橘、`−` 用藍；線上跑的電流記號用紅。
- 按鍵配色要跟著語意走：切到「電流」用 `.btn-red`，切到「電子流」用 `.btn-teal`（就是那個藍）。

---

## 2.3 V5 字體尺度｜統一，不要每區各玩各的

主要可讀文字以 **22～48px** 為 V5 建議範圍。預設字級：

```css
body                { font-size: 22px; }
header .eyebrow     { font-size: 22px; }
h1                  { font-size: 48px; }
header p            { font-size: 24px; }
.board h2           { font-size: 32px; }
.mode-switch button { font-size: 22px; }
.mode-switch .sub   { font-size: 22px; }
.hook-title         { font-size: 26px; }
.hook-text          { font-size: 22px; }
.control label      { font-size: 22px; }
.tool-btn           { font-size: 22px; }
.stat .k            { font-size: 22px; }
.stat .v            { font-size: 28px; }
.explain-box h3     { font-size: 28px; }
.explain-box p      { font-size: 22px; }
footer              { font-size: 22px; }
```

### 原則
- 主要內文字級以 **22px** 為基準，主標題最高以 **48px** 為原則。
- 主要教學文字維持在 **22～48px**；副標題、註解、單位等次要資訊可依版面使用較小字級。
- 同一層級應維持一致字級，不可為了硬塞內容任意縮字。
- 英文版不能因為字串較長就偷偷縮到看不清楚；優先改排版，不是縮字。

---

## 2.4 防重疊規範

### HTML 區塊
- 優先使用 `grid` / `flex` 正常排版。
- 禁止為了塞畫面大量使用 `position:absolute`。
- 長英文按鈕必須允許合理換行，不能硬塞。
- 按鈕尺寸依操作情境、文字長度、畫面密度與裝置調整，不硬套單一固定高度。
- 主要、高頻互動按鈕應保留足夠觸控面積；次要或緊湊控制可適度縮小，但不得造成誤觸或難以閱讀。

### SVG / Canvas
- 圖形與文字之間必須保留安全距離。
- 不得把中文座標直接套給較長英文。
- 英文切換後必須重繪一次。
- 動態文字應在畫圖前估算寬度；必要時左右分流或移到安全區。
- 一旦文字會壓到圖形，優先：**移位置 → 換行 → 縮短文字**；最後才考慮微幅縮字。

### `white-space: nowrap`
只可套在：
- 單一科學名詞
- 公式
- 數值＋單位
- 短按鈕文字

**禁止把整句長英文或整段說明都設成 nowrap。**

---

# 🧮 第三定律｜數理化符號

## 3.1 禁止 MathJax / LaTeX

為避免雙語切換與載入問題：

- 禁用 MathJax
- 禁用 LaTeX `$...$` / `$$...$$`

---

## 3.2 分式

使用純 HTML / CSS：

```html
<span class="fraction">
  <span class="numerator">n₂</span>
  <span class="denominator">n₁</span>
</span>
```

```css
.fraction {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
}
```

---

## 3.3 上下標

- 化學式下標：`<sub>`
- 次方 / 電荷等上標：`<sup>`

例如：
- `H<sub>2</sub>O`
- `cm<sup>2</sup>`

公式、數字、代數預設使用：

```css
.math-text {
  font-family: 'JetBrains Mono', monospace;
}
```

---

## 3.4 物理常數：真實值與計算簡化

- 課本知識、定義與說明一律使用**精確值**，例如 `g = 9.8 m/s²`、`1 kgw = 9.8 N`。學生必須知道真正的數值，升上高中才不會覺得被騙。
- 互動實驗與練習題為了讓國中生好計算，可以取 `g = 10 m/s²`，但**必須在該區塊明確標示「取 g = 10 m/s²」**，不可默默替換。
- 精確值與簡化值**可以並存於同一單元**（概念頁寫 9.8、實驗與計算練習取 10），這是台灣考卷的標準寫法；但不得在沒有標示的情況下混用。
- 其他物理常數同此原則：先給真實值，簡化只發生在計算練習，而且一定要標示。
- **例外：亞佛加厥數一律用國中課本的 `6 × 10²³`**（定義、說明、計算都一樣），不要改成 6.02 × 10²³，也不用加「計算取」的標示——那是高中的程度。
  > 老師原話：「國中生學 6×10²³，你糾正的是高中生的程度。」

---

# ⚙️ 第四定律｜V5 雙語系統

## 4.1 預設語言鎖定繁體中文

載入時畫面必須 **100% 顯示繁體中文**。

英文只存在於：
- `data-en`
- JS 翻譯字典
- `tr('中文','English')`

標準靜態格式：

```html
<div data-zh="中文" data-en="English">中文</div>
```

---

## 4.2 懸浮語言切換器

固定右上角，毛玻璃膠囊設計。

```css
.floating-lang-container {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
}
```

觸控按鈕至少 `44px` 高。

---

## 4.3 Canvas / SVG 雙語

- 動態文字使用 `tr('中文','English')`。
- 切換語言後必須重新繪製。
- 不可為了雙語而重寫整段 Canvas 邏輯。
- 若原本已有成熟 Canvas，優先只替換文字入口。

---

# 👩🏻‍🏫 第五定律｜曉臻老師 Hook

每個主分頁都應有自己的 Hook，不能五頁共用同一句。

Hook 的目的：
- 生活情境
- 認知衝突
- 提問
- 引出本頁科學核心

標準結構：

```html
<div class="hook-box">
  <div class="xiaozhen-logo">
    <img src="assets/characters/xiaozhen/teaching.png" alt="曉臻老師">
  </div>
  <div class="hook-content">
    <div class="hook-title">曉臻老師說：</div>
    <div class="hook-text">...</div>
  </div>
</div>
```

Hook 不能取代教材本體，也不能搶過主動畫。

### Hook 頭像統一格式

- 曉臻老師在 Hook 中固定使用 **88 × 88px 青色圓框頭像**；所有單元統一，不可直接放無框、方形或任意尺寸圖片。
- 圓框使用 `4px solid #55e9ff`、`border-radius: 50%` 與 `overflow: hidden`。
- 圖片使用正式透明角色素材，保持比例並以 `object-fit: cover` 放入圓框；不得拉伸或壓扁。
- Hook 空間極端不足或正式素材載入失敗時，才可使用 `👩🏻‍🏫` 作為後備標記。

```css
.xiaozhen-logo {
  width: 88px;
  height: 88px;
  flex: 0 0 88px;
  overflow: hidden;
  border: 4px solid #55e9ff;
  border-radius: 50%;
}
.xiaozhen-logo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

**Hook 頭像一律用 `teaching.png`。** 其他姿勢（thinking、pointing、surprised、experimenting…）是 320×560 的全身圖，
縮進 88px 圓框只剩一個小小的人、看不到臉；那些姿勢留給字幕、實驗提示這類大尺寸的位置用。
> 老師原話：「曉臻的圖用錯了喔，要用 teaching 那一張。」

### Hook 小單元（分頁鈕）配色 — 全單元統一，不可自行調

分頁鈕就是「Hook 小單元」：每一格是一個子主題的鉤子，主標題寫鉤子、副標題寫學科名稱。
**選中的那一格是整塊實心黃底 ＋ 深色字 ＋ 外圈黃光**，不是「深底配黃字」——
深底配黃字和沒選中的格子對比太弱，學生要找一下才知道自己站在哪裡。

```html
<button class="tab-btn active" onclick="switchTab('tab1', this)" type="button">
  <span class="tab-main" data-zh="機械不省功" data-en="No free lunch">機械不省功</span>
  <span class="tab-sub"  data-zh="功的黃金鐵律" data-en="The golden rule">功的黃金鐵律</span>
</button>
```

```css
.tabs { display: grid; gap: 10px; }          /* 欄數看分頁數：5 格一排、6 格三欄兩列 */
.tab-btn {
  min-height: 136px; padding: 20px 12px; gap: 6px; line-height: 1.4;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  background: rgba(255, 255, 255, 0.05);
  color: var(--chalk-muted);
  border: 2px dashed var(--rule-color);
  border-radius: 10px;
}
.tab-main { font-size: 22px; font-weight: 700; line-height: 1.38; }
.tab-sub  { font-size: 17px; font-weight: 700; line-height: 1.3; opacity: 0.82; }

.tab-btn:hover { background: var(--bg-panel-hover); color: var(--chalk-white); border-color: var(--chalk-white); }

.tab-btn.active {
  background: var(--chalk-yellow);           /* 實心黃底 */
  color: var(--bg-base);                     /* 深色字 */
  border: 3px solid var(--chalk-yellow);
  box-shadow: 0 0 20px rgba(255, 224, 102, 0.5);
}
.tab-btn.active .tab-main,
.tab-btn.active .tab-sub { color: var(--bg-base); }
.tab-btn.active .tab-sub { opacity: 0.78; }

/* 資優／進階分頁：換成珊瑚色，但一樣是實心底 ＋ 深色字 */
.tab-btn.danger-tab.active {
  background: var(--chalk-coral);
  border-color: var(--chalk-coral);
  box-shadow: 0 0 20px rgba(255, 128, 64, 0.5);
}
```

窄螢幕只縮尺寸，不改配色：`min-height` 136 → 118（≤900px）→ 96（≤480px），欄數逐段遞減。

### Hook 對話框底色的陷阱

`.hook-box` 的 8% 青色薄膜**必須疊在「抬起來的面板色」`--bg-panel` 上**。
有些單元的 `.panel` 是透明的，薄膜就會直接落到更暗的 `--bg-base`，整塊 Hook 看起來灰掉、
標題和粗體字都變鈍——電腦上量到的色碼一模一樣，但肉眼就是不對。
所以底色要自己補回去，不要靠父層：

```css
.hook-box {
  /* 不要只寫 background: rgba(77, 210, 255, 0.08); */
  background: linear-gradient(rgba(77, 210, 255, 0.08), rgba(77, 210, 255, 0.08)), var(--bg-panel);
  border: 2px solid rgba(77, 210, 255, 0.4);
  border-left: 6px solid var(--chalk-teal);
  border-radius: 12px; padding: 18px 22px; margin-bottom: 22px;
}
.hook-title { font-size: 26px; font-weight: 700; color: var(--chalk-teal); margin-bottom: 6px; }
.hook-text  { font-size: 22px; line-height: 1.75; color: var(--chalk-white); }
.hook-text b { color: var(--chalk-yellow); }
```

交件前的檢查方式：把新單元和前一個單元的 `.hook-box` / `.tab-btn.active`
用同一支瀏覽器抓 `getComputedStyle`，逐項比對，不要用肉眼判斷「差不多」。

## 5.1 曉臻老師角色母版

曉臻老師是 Physical-Boys 的固定教學角色；語音、字幕、Hook、實驗提示與概念收束應依情境選用她的透明角色圖，不可每個單元重新生成不同長相。

### 固定造型

- 台灣女性自然科老師，親切、自信、具教學感染力。
- 深棕色及肩短髮、側分、髮尾微彎；深棕色眼睛，不戴一般眼鏡。
- 白色及膝實驗袍、橘色上衣、深藍長褲、白色球鞋。
- 與 March、April 使用相容的精緻半立體漫畫風格。
- 新增姿勢時必須以 `assets/characters/xiaozhen_12pose_reference.png` 為母版；臉型、髮型、身材比例、服裝與色彩不得任意改變。

### 正式透明素材

角色素材放在 `assets/characters/xiaozhen/`：

| 檔名 | 建議情境 |
|---|---|
| `teaching.png` | 上課講解、字幕開場 |
| `pointing.png` | 指向公式、圖表或操作重點 |
| `experimenting.png` | 實驗操作與步驟示範 |
| `safety.png` | 安全提醒、重要規則 |
| `praising.png` | 稱讚學生、答對回饋 |
| `encouraging.png` | 鼓勵再試、任務提示 |
| `thinking.png` | 提問、預測、認知衝突 |
| `surprised.png` | 發現異常或實驗結果 |
| `reading.png` | 課本內容、資料解讀 |
| `recording.png` | 觀察紀錄、數據整理 |
| `celebrating.png` | 任務完成、實驗成功 |
| `normal.png` | 一般對話與中性站姿 |

### 使用規則

- 所有角色 PNG 必須保持真正透明背景；禁止白底、假透明棋盤格與 Base64 內嵌。
- 角色圖本身不得加名字、字幕或說明文字；中英文字幕放在 HTML 文字層，維持同一張無字圖片共用。
- 保持原始長寬比，使用 `object-fit: contain`；不得拉寬、壓扁或裁掉手勢與實驗器材。
- 語音與字幕段落應依語意切換合適姿勢，不可只寫「曉臻老師」卻沒有角色圖；空間不足的極小型 Hook 才可保留 `👩🏻‍🏫` 作為後備標記。
- 若需要左右觀看方向，可用 CSS 水平鏡像；不得為此另畫一張不同造型。

---

# 🎬 第六定律｜電影式單元封面

固定模式：

```html
<div id="introScreen" class="intro-screen"></div>
```

### 規定
- Cover 畫面上 **不得出現任何文字**，包括單元標題、副標題、提問、公式、標籤、「點擊進入」與操作提示。
- Cover 只用影像製造懸念，讓學生先觀察、猜想與提問；答案與教學文字必須等 Cover 退場後才出現。
- 封面原始圖片本身也不得生成或後製任何文字、數字、公式、標籤、Logo 或浮水印。
- 圖片使用相對路徑，例如：

```css
background: url("optics_3_cover.jpg") center / cover no-repeat;
```

- 禁止 Base64 圖片。
- **影片封面的 `poster` 一定要用影片的「第一格」，絕對不能用最後一格或高潮那一格。**
  封面是點一下才播（不 autoplay、不 muted），播之前那張靜止畫學生會盯著看好幾秒；
  放結局等於先把答案講完，懸念就沒了。用第一格還有一個好處：點下去的瞬間畫面完全接得上，
  不會跳一下。做法是從成品影片抽第 0 格：
  `ffmpeg -i <cover>.mp4 -vf "select=eq(n\,0)" -frames:v 1 -q:v 3 <cover>.jpg`
  —— `poster` 和 `.intro-screen::before` 的背景圖要用同一張。
- 動畫採緩慢推鏡 `introZoom`。
- 搭配暗角或底部漸層遮罩。
- 不可因封面造成 iOS 邊線、滾動條或版面位移。

---

# ⚡ 第六定律之二｜電路動畫一律用「電子流」

顏色依 **2.2.1 電學語意色**：電子藍 `#4dd2ff`、電流紅 `#ff4d4d`。

電路題材的動畫，跑在線上的球**一律代表電子，一律從電池的負極出發**，
繞一圈在正極回去。不可以這一頁用電子流、那一頁用電流方向。
（分頁裡專門在比較「電流 vs 電子流」的那一格是唯一例外，
　而且兩種一定要用 2.2.1 的顏色分開：電子藍、電流紅。）

## 三條鐵律

1. **球在跑的方向，和能量標的方向，必須一致。**
   這是最容易出錯、也最難自己看出來的地方——如果球是從正極往右跑，
   能量卻標在左下角，畫面就自相矛盾，學生一定會發現。

2. **能量是在「元件」上被吃掉的，不是在電線上慢慢遞減的。**
   兩個元件之間，球的能量完全不變；經過燈泡／電阻／電表才掉一段。
   所以能量的落點要用「元件的座標」去算，不可以用 `x > 480 ? 高 : 低`
   這種座標硬猜——那種寫法換一個版面就錯，而且永遠對不準。

   ```js
   // 對：把元件的座標交給程式，讓它自己算在路徑上的位置
   e_flow(ctx, loop, 52, shift, [{ x: BULB_X, y: TOP, amount: 1 }]);
   // 串聯兩顆燈泡，各吃一半
   e_flow(ctx, loop, 54, shift, [{ x: 330, y: TOP, amount: 0.5 },
                                 { x: 630, y: TOP, amount: 0.5 }]);
   ```

3. **畫面上一定要有圖例**：一顆球 ＋「藍球＝電子，從負極出發」。
   畫面上只有球在動，學生沒辦法自己猜出那是電子還是電流，必須連顏色一起寫出來。

## 支路的轉角不可以留空

並聯的每一條支路，路徑要含**兩端轉角的那兩小段直立導線**，
否則電子跑到角落就憑空消失。學生盯著看一定會發現。

```js
// 錯：只有水平那一段，電子會在左右兩個轉角消失
const pTop = [{x: L, y: TOP}, {x: R, y: TOP}];
// 對：含轉角，整圈連續
const pTop = [{x: L, y: MID}, {x: L, y: TOP}, {x: BULB, y: TOP}, {x: R, y: TOP}, {x: R, y: MID}];
```

## 元件要畫在電子之後

先畫導線 → 再放電子 → **最後才畫元件**。順序反了，開關、電表、電池會整個被電子串蓋掉。

## 導線的缺口要對上符號的接腳

`drawBatterySymbol` 這類符號自己會畫出 `±30 × size` 的接腳，
導線就要畫到那個位置為止。缺口隨便開，看起來就是「線沒有接好」。

## 斷開就要真的斷開

開關打開、導線被剪斷、電路故障——這些「不通」的狀態，
**導線本身一定要留出真正的缺口**，不可以把一個開關符號或一個紅叉
疊在一條完整的導線上就算數。學生看到的是那條連續的線，不是那個符號。

```js
// 錯：整條左幹線畫到底，再把開關疊上去 —— 看起來還是通的
e_wire(ctx, [{x: 420, y: B}, {x: L, y: B}, {x: L, y: TOP}]);
e_switchSym(ctx, L, SY, false);
// 對：幹線分兩段，缺口留給開關
e_wire(ctx, [{x: 420, y: B}, {x: L, y: B}, {x: L, y: Y_ON}]);
e_wire(ctx, [{x: L, y: SY + 26}, {x: L, y: TOP}]);
e_switchSym(ctx, L, SY, false);
```

**而且要順著電流從電池走一遍**，確認這個缺口切掉的正好是題目說的那幾條支路。
支路是疊在同一條幹線上的時候，開關只能切斷「比它更遠離電池」的那一段——
想讓某一條支路獨自通電，那一條就必須是離電池最近的那一條。
座標排好看不等於接線是對的。

## 剖面圖只有剖開的那一段看得到裡面

畫剖面（導線、電表、燈泡）時，外殼、銅芯、內部零件的繪製範圍都要以切口為界：
沒剖開的那一段必須是完整的外觀。左右兩段長得一模一樣，那條切口線就只會被
當成「這根線被剪斷了」。

---

## 元件要站在導線的正中央，整張圖要以畫布中線長開

兩條規矩，都是「看起來醜不醜」之前先問「對不對稱」：

1. **用電器（燈泡、渦輪、電表）一律擺在它所在那條導線的正中央。**
   為了閃開旁邊的標註而把燈泡往旁邊推，學生第一眼就看得出那張圖歪了。
   要讓路的是標註，不是元件——把箭頭、尺標移到別的位置，元件不要動。
   > 事故：並聯那一張為了閃開電壓箭頭，把燈泡從導線中心 734 推到 790。
   > 老師一眼就說「有點醜，電器我們都畫在導線的中央」。

2. **會隨參數變大變小的圖，要以畫布中線對稱長開，不要以底邊為基準往上長。**
   以底邊為基準，參數小的時候整組圖黏在畫布底下、上半張全空；
   參數大的時候才勉強填滿。改成從中線往上下對稱長，每一種狀態都落在正中間。

   ```js
   // 錯：以底邊為準，一段的時候整組黏在下面
   const LOW = 448, HIGH = LOW - STEP * n;
   // 對：以中線為準，往上下對稱長開
   const HIGH = MID - STEP * n / 2, LOW = MID + STEP * n / 2;
   ```

   附帶好處：左右兩張對照圖（水位差 vs 電壓）的尺標會自動落在同一個高度，
   眼睛直接連得起來，不必再一個一個微調座標。

## 並排的東西，不可以沿用疊高的座標

串聯（疊高）和並聯（並排）是兩種完全不同的版面。
把串聯那張的座標複製過來改兩個數字，一定會生出歪掉的並聯圖——
串聯只有一根柱子，並聯有兩根要左右對稱；串聯的標註擠在單邊沒問題，
並聯的標註夾在兩根中間就會壓到東西。

**同一個概念的不同接法，版面各算各的。** 共用的是繪圖函式與配色，不是座標。

# 📱 第七定律｜iPad Pro 13 與響應式

## 7.1 1080px 仍是主框架

iPad Pro 13 的意思是「在 1080px 主框架內充分利用空間」，不是把整頁無限拉寬。

## 7.2 Canvas / SVG

保留原始內部解析度與幾何比例。

外層可使用：

```css
canvas,
svg {
  width: 100%;
  height: auto;
}
```

若原圖已有固定比例，再加對應 `aspect-ratio`。

### 禁止
- 非等比拉伸
- 擅自改 Canvas 內部座標系
- 為了滿版而改物理幾何

## 7.3 直式裝置

必要時於窄螢幕改成單欄，但不得破壞原動畫。

---

# 🧠 第八定律｜教學敘事

優先採用：

> 現象 → 疑問 → 認知衝突 → 模型 → 操作／驗證 → 概念收束

主分頁標題要直接打中科學問題，避免「第一章、第二步、一二三」式標題。

## 考題的附圖不可以先把答案畫出來

考題頁的電路圖是**題目的一部分**，不是解答的一部分。
「哪一段斷路？」的圖上就不可以出現缺口、紅叉、或某一條支路停住不動的電子——
那等於把答案直接標給學生看。這種圖要畫**事件發生前的正常狀態**，
再用一句話把題目的情境說清楚（例：「圖中是碰撞前的正常電路」）。

## 考題附圖一律用「考卷上的標準電路圖」，不放電子動畫

學生考試時拿到的是紙本考卷，上面只有標準電路符號。前面的分頁已經用電子動畫把觀念建立起來了，
到了考題就要讓學生練習**讀標準電路圖**：

- 導線用細直線、電阻用鋸齒符號、電池用「長細線＝正極、短粗線＝負極」，並聯節點畫實心圓點。
- 不放電子、不放圖例、不放 3D 電池；題目給的數值（電壓、電阻、電流）照考卷的習慣標在元件旁邊。
- 這種圖是**靜態的**，10.3「畫布要動」在這裡豁免；驗證時反過來要確認它真的不動。
- 附圖放在題目正上方（題號 → 圖 → 題目 → 選項），不要跟題目隔開——圖是用來降低認知負擔的。
  > 老師原話：「這裡不要改成我們的動畫，因為學生考試沒有動畫。前面已經出現很多了，可以用正常的電路圖了。」

例如：
- 「為什麼會折射？」
- 「速度變了多少，我們怎麼看得見？」
- 「這條公式，是怎麼從圖裡長出來的？」

練習題如需新增或重排，採：

> Question → Hint → Answer

但題庫屬 YELLOW / RED 邊界，未經老師同意不要動原題。

---

## 8.1 學生數得出來的「段數」，勝過連續的滑桿

一條 1～5 的滑桿，對寫程式的人是「參數可調」，對國中生是**沒有單位的抽象數字**。
學生看得懂的是**可以用手指頭數的段數**：一顆電池就是一組幫浦抬一段，
兩顆就是兩組幫浦接力抬兩段。段數和實物一對一，學生才推得動。

### 規定

- 教學模型的控制項，優先做成**離散、可數、和實物一一對應**的選項，
  而不是連續滑桿。滑桿留給「趨勢」類的探究（例如外電阻由小到大看端電壓怎麼垮）。
- 選項數量以**畫面塞得下且不縮小元件**為上限。塞不下就少做一個，
  不要為了多一個選項把所有東西縮小。
  > 老師原話：「就用最簡單的只有兩段，一顆和兩顆電池（三顆會放不下）。」
- **「老師看得懂」不等於「學生看得懂」。** 模型抽象到需要老師在旁邊解釋才成立，
  這個模型就還沒做完。交付前先問一次：把講解拿掉，學生自己看這張圖，推得出來嗎？

# 🧪 第九定律｜科學圖像與公式的教學定位

- 圖不是裝飾，是教材內容。
- 動畫不是特效，是思考模型。
- 原有實驗裝置不可因「美化」而刪除。
- 公式不得取代理解過程。
- 若一個概念原本依靠圖、動畫、操作建立，就必須保留該路徑。

特別是運動學：

> **先把運動畫出來，題目才真正看懂。公式不是取代圖，而是把圖上的關係寫得更精簡。**

---

# 🛡️ 第十定律｜動畫與 iOS 穩定性

- `requestAnimationFrame` 必須避免重複啟動。
- 多分頁動畫若有各自 RAF，切頁時要取消舊 RAF。
- 若頁面已有 `killAllSims()`，必須保留並正確清除 RAF ID。
- 不得在 HTML inline handler 依賴隱含 `event` 物件。
- Canvas 初始化前確認尺寸有效。
- iOS 若有黑畫面、垂直線、邊線問題，優先修 CSS / compositing，不可直接刪掉封面或動畫。

---

## 10.1 動畫的「續播條件」是分頁編號的地雷

多分頁教材的動畫幾乎都長這樣，最後一行決定它會不會繼續播：

```js
function animateX() {
  // ...畫這一格...
  if (currentTab === 'tab4') rafX = requestAnimationFrame(animateX);
}
```

只要**插入新分頁或調整分頁順序**，這個寫死的分頁編號就會對不上。

症狀非常隱蔽：該分頁**畫得出第一格，然後完全不動**。
不會報錯、畫面也不是空白，自動檢查和肉眼快速掃過都抓不到，
通常要等到老師上課當場才發現。

### 規定

- 任何分頁的**增加、刪除、重新排序**之後，必須把整份檔案裡
  **每一個** `currentTab === '...'`（或等效的分頁判斷）全部列出來逐一核對。
- **只改 `switchTab()` 不算數**——真正會出事的是各個動畫函式自己尾巴那一行。
- 核對完必須用 10.3 的方法實測，不可只用讀的。

---

## 10.2 所有被呼叫的函式都必須真的存在

`oninput=` / `onclick=` 這類行內處理器呼叫的函式如果不存在，
瀏覽器只會在使用者真的去點的那一刻才丟 `ReferenceError`。

更糟的是：如果同一個函式**同時也被初始化或 `setLang()` 呼叫**，
那一行就會讓它後面的程式**整段停止執行**，連帶拖垮其他功能。

### 規定

交件前必須把所有行內處理器與 JS 內呼叫到的函式名稱抓出來，
與檔案裡實際定義的函式清單比對，
確認沒有任何一個是「只有呼叫、沒有定義」。

---

## 10.3 交件前必須實測「動畫有沒有在動」

這是本定律最重要的一條，因為它補的是**檢查方法本身的漏洞**：

> **「沒有 JS 錯誤」和「畫布上有東西」，都無法偵測卡住的動畫。**
> 停在第一格的畫面，這兩項檢查都會通過。

### 規定

每一張**應該會動**的畫布，都必須實測
**同一張畫布間隔約 1 秒前後的像素差異**，確認差異明顯大於 0。

- 按鈕觸發型的互動（例如「按下才發射」「按下才煞車」）要**先觸發再量**；
  這類畫面閒置時不動是正常的，不可誤判為故障。
- 靜態示意圖（畫一次就好的圖解、題目附圖）不適用本條，
  但必須在交付說明中指出哪些是靜態的，避免下一次被誤判成壞掉。
- 語言切換後要再量一次，確認切換沒有把畫面打斷。

---

# 📝 第十一律｜工作 SOP

收到「開始 / GO / 下一個 / XX-1 開始」即代表已授權執行該指定範圍。

標準流程：

1. 讀取目前 GitHub 最新檔案。
2. 確認老師指定範圍。
3. RED ZONE 不動。
4. 採最小修改。
5. 修改後重新讀取或比對關鍵區段。
6. 確認：
   - 1080px 沒被改掉
   - 中文預設正常
   - EN 可切換
   - 原動畫仍在，而且**實測過它真的在動**（見 10.3）
   - 若動過分頁結構，所有 `currentTab === '...'` 已逐一核對（見 10.1）
   - 沒有「只有呼叫、沒有定義」的函式（見 10.2）
   - 原圖形仍在
   - 無新增文字重疊
   - iPad 控制至少 44px
7. Commit。
8. 回報改了什麼、沒動什麼、commit SHA。

---

# 📦 第十二律｜完整交付

若使用者要求完整 HTML：
- 必須提供完整可獨立執行檔。
- 禁止使用 `<!-- 略 -->`、`// omitted` 等省略。
- 未修改區段保持原樣。

若直接修改 GitHub：
- 只修改老師指定的檔案。
- 不順手整理其他檔案。
- 不順手改內容。
- 不順手重構。

---

# ♻️ 第十三律｜預覽快取與新版本交付

每次提供「修正版預覽」時，必須預設瀏覽器、Sandbox 或 iOS 可能仍保留上一版快取；不可只覆蓋舊檔後，再把同一條預覽連結交給老師。

## 13.1 預覽檔必須使用全新檔名

- 每次有實質修正，檢查用預覽必須建立新的唯一檔名。
- 檔名至少包含：單元、功能、日期或遞增版本號。
- 例如：`mechanics_2_force_table_3d_preview_0827_v2.html`。
- 禁止更新 `preview_v1.html` 的內容後，仍把原本的 `preview_v1.html` 連結當成新版本交付。
- 正式教材檔名維持不變；只有本機／Sandbox 檢查版需要遞增檔名。

## 13.2 防快取標頭只是第二層保護

正式頁面與預覽頁可加入：

```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
<meta http-equiv="Pragma" content="no-cache">
<meta http-equiv="Expires" content="0">
```

但不得只依賴上述標頭。**全新預覽檔名才是避免老師看到舊版的主要方法。**

## 13.3 獨立預覽的素材規則

- 若 Sandbox 單檔預覽無法帶入相對路徑素材，檢查版必須把 Cover 與必要圖片內嵌，或建立能完整載入素材的獨立預覽。
- 正式 Git 版本仍遵守相對路徑與禁止 Base64 的規範；不可把巨大內嵌預覽檔推上 GitHub。
- 不可把「圖片路徑失效」誤判成「瀏覽器快取」；必須分別檢查 HTML 版本、圖片載入與檔名快取。

## 13.4 交付前核對

提供新預覽連結前必須確認：

1. 新預覽檔名從未交付過。
2. 預覽內容含本次修改的明確特徵。
3. JavaScript 語法正常，必要素材能載入。
4. 連結指向新檔，而不是上一版或正式舊檔。
5. 未經老師檢查與明確同意，不得因為「已完成預覽」而自行推上 GitHub。

---

# ✅ V5 最後檢查清單

每次交付前逐項確認：

- [ ] 主內容 `max-width: 1080px`
- [ ] Cover 畫面與封面原圖完全無字
- [ ] 核心互動沒有被雙欄縮小；必要時採 1080px 框架內全寬單欄
- [ ] 未碰 RED ZONE
- [ ] 預設繁體中文
- [ ] 中英切換正常
- [ ] 字體層級符合 V5
- [ ] 沒有長英文硬塞造成重疊
- [ ] 沒有文字壓到 SVG / Canvas 圖形
- [ ] 控制按鈕至少 44px
- [ ] 沒有純黑 `#000`
- [ ] 沒有 MathJax / LaTeX
- [ ] Canvas / SVG 等比例
- [ ] 原動畫、原實驗、原題目仍存在
- [ ] 每一張應該會動的畫布，已實測 1 秒內確實有像素變化（不是只看「沒報錯」）
- [ ] 分頁有增刪或重排時，所有動畫的 `currentTab === '...'` 已逐一核對
- [ ] 沒有任何函式「只有呼叫、沒有定義」
- [ ] 測試瀏覽器已確認載到 JetBrains Mono 與 Noto Sans TC（見 10.5）
- [ ] 畫布上每一行字都已實測「拿掉它畫面會變」，沒有連續 4px 以上看不到（見 10.4）
- [ ] 寫在色塊裡的字已用 fitText 量過，並在交付說明中寫出最後的字級（見 1.1）
- [ ] 會被水／方塊／面板蓋住的線，已確認排在那些東西之後畫
- [ ] 若做了視覺升級，已附上「所有數值座標完全相同」的比對結果（見 1.1）
- [ ] iPad Pro 13 橫式正常
- [ ] iPad 直式不破版
- [ ] 本次修正版使用全新、未交付過的預覽檔名
- [ ] 新預覽已確認包含本次修改，且不是舊檔快取
- [ ] 獨立預覽所需 Cover／圖片素材能正常載入
- [ ] 未經老師明確同意，沒有推上 GitHub
- [ ] Commit 前已再次檢查差異

---

## 10.4 「畫了」不等於「看得到」

`ctx.fillText()` 有沒有被呼叫，跟那行字在螢幕上看不看得到，是**兩件事**。
中間隔著兩個殺手：

| 殺手 | 長什麼樣子 |
|---|---|
| **顏色等於背景** | 深色字寫在亮色塊上，字一旦超出色塊就變成同色同底，整段隱形 |
| **被後面畫的東西蓋掉** | 先畫的線，被後畫的不透明色塊塗掉（水、方塊、面板） |

兩個都通過「JS 零錯誤」「畫布上有東西在動」「畫布沒有破版」。
它們**只有把每一筆單獨拿掉、看畫面有沒有變，才抓得到**。

### 規則

1. 畫布上每一筆下筆，都要能證明「拿掉它之後，合成到真正的背景上，人眼看得出差別」。
   - **文字**：只要有**連續 4px 以上完全看不到**，就是不合格，沒有商量。
   - **非文字**：連續 8px 以上看不到就列出來人工判斷
     （能量條的凹槽被填充蓋住是刻意的，要人看過才算數）。
2. **比像素有沒有變是不夠的。** 畫布是透明底，
   把透明畫成 `#11261f` 在像素上「有變」，合成到黑板底色上卻是一模一樣。
   一定要**先合成到畫布背後真正的底色**再比對比度。
3. 會被水、方塊、面板蓋住的線，**要排在那個東西之後畫**。
   畫圖時先問一句：「這條線等一下會不會被什麼蓋掉？」

### 作法

把每一筆編號，對第 k 筆各畫三次：

| 畫法 | 得到 |
|---|---|
| 畫到第 k−1 筆 vs 畫到第 k 筆 | 第 k 筆**本來想畫**的範圍 |
| 完整畫 vs 完整畫但跳過第 k 筆 | 第 k 筆**最後真的看得到**的範圍 |

兩者相除就是可見率。再把可見率**按直行拆開**看有沒有整段死區——
抗鋸齒是全圖均勻掉 4%，被蓋掉／出界是一整段連續的行完全不見，
只看總比例分不出這兩種，看死區長度才分得出來。

---

## 10.5 驗證一定要用「老師螢幕上真正的那套字」

> 我在測試瀏覽器裡量到那行公式是 **133px**，判定「塞得下」。
> 老師的 Mac 上它是 **165px**，超出膠囊 20px。
> 差別只有一個：測試瀏覽器**沒有 JetBrains Mono**，它退回了預設字體。

字寬換一套字就變，所以**所有跟「塞不塞得下、會不會壓到、會不會被裁掉」有關的驗證，
在字體沒載到的環境裡跑，結論一律無效**。

### 規則

1. 跑排版驗證之前，先確認測試瀏覽器**真的載到了** `JetBrains Mono` 與 `Noto Sans TC`
   （`document.fonts.check('bold 19px "JetBrains Mono"')`）。
2. **不准把字體載入失敗的錯誤濾掉。**
   「這是環境雜訊」是我當初寫下 `/fonts\.g/` 這個過濾條件時的想法，
   結果那行過濾把唯一的線索丟掉了。
   字體載不到 = 這次的排版結論全部作廢，要講出來，不是吞掉。
3. 驗證環境連不到 Google Fonts 時，用 `@fontsource` 的**同一批 woff2**
   在本機補上，把 `fonts.googleapis.com` 的請求接下來改餵本機檔案。

---

## V5 一句話總綱

> **不是把課本電子化，而是把好老師腦中的教學路徑程式化；AI 的工作是保護這條路徑，再把它表達得更清楚。**
