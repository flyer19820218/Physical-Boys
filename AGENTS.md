# 黎明國中自然互動網站開發規範

本檔適用於整個 `Physical-Boys` 倉庫。除非使用者在當次任務明確覆蓋，所有開發、重構、雙語化與排版工作都必須遵守。

## 1. 最高原則：鎖定物理與動畫

- 雙語化、排版與相容性工作不得改動物理內容或動畫邏輯。
- 不得擅自改動物理常數、方向、週期、速率、座標、路徑、顏色語意、動畫先後順序、按鈕行為、計時器、`requestAnimationFrame`、Canvas 繪圖演算法或互動狀態。
- 已完成的教學設計（例如 1 Hz 同步、渦電流、立體切線箭頭、右手定則、滑環與集電環順序）視為鎖定資產。
- 可以檢查並列出疑似物理或動畫 Bug，但未經使用者明確同意，不得修正。
- 若排版需求必須觸及 Canvas 座標或動畫程式，先停止並說明原因、影響範圍與最小修改方案。
- 不以「順便優化」為理由重寫可正常運作的程式。

## 2. 工作流程與修改範圍

- 一次只處理使用者指定的單元或 HTML；不要掃描或重構無關檔案。
- 修改前先建立基準：確認分頁、子分頁、按鈕、滑桿、動畫、語言切換與 Console 狀態。
- 優先做最小差異修改，保留原有 HTML 結構、CSS 命名與 JavaScript 架構。
- 不覆蓋使用者或其他工具已完成的無關修改。
- 先產生本機可直接開啟的預覽供驗收；未獲同意不得推送 GitHub。
- 使用者確認後，才更新正式檔案、`index.html` 與 `README.md`（僅在任務需要時），並建立 PR。
- GitHub 作業強制使用已連線的 GitHub App／合併管道；禁止使用 GitHub CLI（`gh`）進行認證、推送、建立 PR 或合併。
- 固定發布流程：建立 `agent/...` 分支 → 僅提交本次確認範圍 → 建立 PR → 等使用者明確說「合併」後才合併。
- 禁止直接推送或合併到 `main`，也不得要求使用者重新進行 GitHub 認證。
- 本機交付完整、可獨立執行的 HTML；聊天回覆只提供檔案連結與修改摘要，不貼整份程式碼，除非使用者明確要求。

## 3. 雙語架構

### HTML 靜態文字

- 所有可見介面文字使用 `data-zh` 與 `data-en`，元素內預設放中文：

```html
<div data-zh="中文" data-en="English">中文</div>
```

- 包含標題、分頁、子分頁、按鈕、提示、解答、圖說、ARIA 標籤及狀態訊息。
- 中文模式只顯示中文；英文模式只顯示英文，不混用兩種語言。
- 切換語言時同步設定 `<html lang="zh-Hant">` 或 `<html lang="en">`。
- 右上角使用清楚的 `Language／語言` 切換控制，按鈕需顯示目前狀態。
- 同一套動畫共用一份程式，不複製中文版與英文版 HTML。

### Canvas 動態文字

- 所有 Canvas 文字透過既有 `tr(zh, en)` 翻譯函數輸出：

```js
label(ctx, tr('熱能', 'Heat'), x, y);
```

- 不翻譯國際通用物理符號，如 `B`、`Bᵢ`、`I`、`Iₚ`、`F`、`e⁻`，但其說明文字必須雙語。
- 英文採精簡、標準的科學術語，例如 `Induced B`，避免長句壓到圖形。
- 英文較長時，優先縮短措辭；必要時才局部換行或微調字級，不改動畫圖形與座標。

## 4. 視覺與顏色

- 背景固定為深黛綠點狀網格 `#11261f`。
- 全站與 Canvas 禁止使用純黑 `#000` 或 `black`；深色區使用深黛綠系。
- 使用高對比粉筆色：白、亮黃、亮青、亮橘、草綠、粉紅。
- 文字說明與對應動畫圖形必須使用一致的顏色語意。
- 既有物理顏色規則不得改動；例如專案既定的紅色電子流、藍色傳統電流。
- 保持簡潔，避免裝飾造成額外認知負荷。

## 5. 排版與字體

- 主內容最大寬度 `1080px`，水平置中並保留兩側空間。
- 一般內容文字固定 `20px`；標題依層級使用 `28px` 至 `48px`。
- 數理公式與代數使用 `JetBrains Mono`。
- 科學專有名詞與短公式使用 `white-space: nowrap`，不得從中間斷字。
- 數學分數不得使用斜線，必須以 CSS 製作上下垂直分式。
- 分頁標題直接呈現科學概念，不加「一、二、三」等前綴。
- 單元原則上維持 4 至 5 個主分頁；不得為了雙語化改變分頁結構。

## 6. iPad Pro 與響應式設計

- 寬度大於 `768px`：維持 `grid-template-columns: 1fr 1fr` 雙欄。
- 寬度小於或等於 `768px`：切換為單欄上下排列，禁止裁切或水平溢出。
- iPad Pro 13 吋橫向須在 `1080px` 主容器內充分使用空間；直向須保持內容完整與易讀。
- Canvas／互動區內部解析度固定（建議 `1000 × 600`），外部使用：

```css
width: 100%;
height: auto;
aspect-ratio: 10 / 6;
```

- 禁止以非等比例 CSS 拉伸 Canvas。
- 所有互動控制至少提供約 `48px × 48px` 的觸控熱區；滑桿與按鈕需適合手指操作。
- viewport 使用 `width=device-width, initial-scale=1, viewport-fit=cover`；按鈕可使用 `touch-action: manipulation` 避免誤觸放大。
- 必須檢查 iPad 橫向、iPad 直向、桌面及手機直向；特別檢查 iOS Safari 的溢出、白線與安全區問題。

## 7. Canvas 穩定性與防碰撞

- HTML 標籤內不得依賴隱含的 `event` 物件；事件監聽使用明確參數或 `addEventListener`。
- Canvas 與 DOM 尺寸讀取需延遲到版面完成後，且先確認寬、高大於 0。
- 初始化與 resize 必須避免重複建立動畫迴圈或計時器。
- Canvas 文字、引線、標籤與圖形不得重疊；以 `1000px` 內部寬度精算左右分流與安全間距。
- 語言切換後重新量測文字寬度並重繪，不可只替換字串。
- 不得因響應式排版改動原有物理圖形比例或動畫路徑。

## 8. 教學內容

- 語氣直白、專業、精簡，避免贅語。
- 不加入 `cite 1`、`cite 2` 等占位字樣。
- 練習題固定採三階段：`問題 → 提示 → 解答`。
- 雙語翻譯必須保持物理意義與程度，不自行增刪教材內容。
- 發現疑似科學錯誤時，列出位置、目前內容與建議，等待使用者決定。

## 9. 檔名與驗收

- 檔名使用與內容相關的英文名稱，格式為 `topic_1.html`、`topic_2.html` 等。
- 修改後至少驗證：
  - 所有主分頁與子分頁可切換。
  - 中文與英文完整切換，沒有漏翻或混語。
  - Canvas 在切換語言與旋轉螢幕後仍正常重繪。
  - 播放、暫停、重播、滑桿及觸控操作正常。
  - Console 無錯誤，沒有重複動畫迴圈。
  - iPad Pro 橫向雙欄、直向及手機單欄皆不裁切、不變形、不重疊。
  - 與修改前相比，物理數值、方向、時序與動畫行為完全一致。
